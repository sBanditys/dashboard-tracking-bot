---
phase: 03-tracking-data-display
plan: 06
type: execute
wave: 4
depends_on: ["03-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can view tracked accounts in card format"
    - "User can scroll to load more accounts"
    - "User can search and filter accounts"
    - "User can view tracked posts in card format"
    - "User can scroll to load more posts"
    - "User can filter posts by platform, date, and status"
    - "User can view brands with expandable details"
    - "Empty states guide user appropriately"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 3 tracking data display functionality works correctly.

Purpose: Human verification that the card-based layouts, infinite scroll, filtering, and empty states all function as designed before marking Phase 3 complete.

Output: Verified tracking data display or list of issues to address.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-tracking-data-display/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Accounts Page</name>
  <what-built>
Card-based accounts view with infinite scroll, search, platform filter, and page size selector.
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Log in and navigate to a guild's accounts page
3. Verify card layout:
   - Cards show platform icon, username, brand, status
   - Cards are in responsive grid (3 cols on desktop, 1 on mobile)
4. Click a card - verify it expands to show additional details
5. Scroll down - verify more accounts load automatically
6. Use search - verify instant filtering (debounced)
7. Use platform dropdown - verify filter works
8. Change page size - verify batch size changes
9. Apply filter that matches nothing - verify NoResults with clear option
10. Verify ScrollToTop button appears when scrolled down
  </how-to-verify>
  <resume-signal>Type "accounts-approved" or describe issues found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Posts Page</name>
  <what-built>
Card-based posts view with infinite scroll, search, platform filter, date range picker, status filter, and page size selector.
  </what-built>
  <how-to-verify>
1. Navigate to a guild's posts page
2. Verify card layout:
   - Cards show platform, author, timestamp, status, metrics preview
   - Cards are in responsive grid
3. Click a card - verify it expands to show full metrics
4. Scroll down - verify more posts load automatically
5. Use search - verify instant filtering
6. Use platform dropdown - verify filter works
7. Use date range picker:
   - Click to open calendar
   - Select a date range
   - Verify posts filter to that range
8. Use status filter - verify filter works
9. Combine multiple filters - verify they work together
10. Clear all filters - verify reset works
  </how-to-verify>
  <resume-signal>Type "posts-approved" or describe issues found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Brands Page and Empty States</name>
  <what-built>
Brands view with expandable cards and proper empty states across all tracking pages.
  </what-built>
  <how-to-verify>
1. Navigate to a guild's brands page
2. If brands exist:
   - Verify expandable cards work
   - Click to expand, see account groups
3. If no brands (or find a guild with none):
   - Verify EmptyState shows with icon, title, description
   - Verify CTA button links to help/docs

4. Test empty states on accounts and posts:
   - If guild has no accounts, verify EmptyState shows
   - If guild has no posts, verify EmptyState shows
   - These may require a test guild with no data

5. Verify sticky filter bar:
   - Scroll down on accounts or posts page
   - Verify filter bar stays at top (sticky)
   - Verify backdrop blur effect
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All tracking data display requirements verified:
- TRACK-01: Accounts viewable with pagination (infinite scroll)
- TRACK-02: Posts viewable with pagination (infinite scroll)
- TRACK-03: Brands viewable within guild
- TRACK-04: Search and filter working
</verification>

<success_criteria>
- All three checkpoints approved
- Cards expand/collapse smoothly
- Infinite scroll loads data without duplicates
- Filters work individually and combined
- Empty states show appropriate guidance
- ScrollToTop appears and works
- Responsive layout works on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-tracking-data-display/03-06-SUMMARY.md`
</output>
