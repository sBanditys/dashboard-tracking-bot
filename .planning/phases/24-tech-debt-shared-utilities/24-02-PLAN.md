---
phase: 24-tech-debt-shared-utilities
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validators.ts
  - src/app/(dashboard)/guilds/[guildId]/posts/page.tsx
  - src/lib/format.ts
  - src/hooks/use-bonus.ts
  - src/components/bonus/round-card.tsx
  - src/components/bonus/create-round-modal.tsx
  - src/components/bonus/payments-tab.tsx
  - src/components/bonus/results-tab.tsx
  - src/components/bonus/leaderboard-tab.tsx
autonomous: true
requirements: [DEBT-03, DEBT-04]

must_haves:
  truths:
    - "validators.ts no longer exists in the codebase"
    - "Posts page shows ConnectionIssuesBanner when SSE connection is degraded"
    - "A shared centsToDisplay function exists in src/lib/format.ts"
    - "All 5 bonus components import centsToDisplay from @/lib/format (not from use-bonus)"
    - "use-bonus.ts no longer exports centsToDisplay"
  artifacts:
    - path: "src/lib/format.ts"
      provides: "Shared centsToDisplay utility"
      exports: ["centsToDisplay"]
    - path: "src/app/(dashboard)/guilds/[guildId]/posts/page.tsx"
      provides: "ConnectionIssuesBanner wiring"
      contains: "ConnectionIssuesBanner"
  key_links:
    - from: "src/components/bonus/round-card.tsx"
      to: "src/lib/format.ts"
      via: "import { centsToDisplay }"
      pattern: "from '@/lib/format'"
    - from: "src/app/(dashboard)/guilds/[guildId]/posts/page.tsx"
      to: "src/components/connection-issues-banner.tsx"
      via: "import and render"
      pattern: "ConnectionIssuesBanner"
---

<objective>
Remove dead code, wire ConnectionIssuesBanner to posts page, and extract the shared formatCents utility.

Purpose: Delete validators.ts (zero imports), add the missing ConnectionIssuesBanner to the posts page (matching the accounts page pattern), and extract centsToDisplay to a shared module so campaign phases can import it without depending on bonus hooks.
Output: Dead code removed (DEBT-04), banner wired (DEBT-03), shared utility ready for Phase 25+ (formatCents extraction).
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-tech-debt-shared-utilities/24-CONTEXT.md
@.planning/phases/24-tech-debt-shared-utilities/24-RESEARCH.md

@src/lib/validators.ts
@src/app/(dashboard)/guilds/[guildId]/posts/page.tsx
@src/app/(dashboard)/guilds/[guildId]/accounts/page.tsx
@src/components/connection-issues-banner.tsx
@src/hooks/use-bonus.ts
@src/components/bonus/round-card.tsx
@src/components/bonus/create-round-modal.tsx
@src/components/bonus/payments-tab.tsx
@src/components/bonus/results-tab.tsx
@src/components/bonus/leaderboard-tab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete validators.ts and wire ConnectionIssuesBanner to posts page (DEBT-03, DEBT-04)</name>
  <files>src/lib/validators.ts, src/app/(dashboard)/guilds/[guildId]/posts/page.tsx</files>
  <action>
**DEBT-04: Delete validators.ts**
Delete the file `src/lib/validators.ts` entirely. It has 6 exports (emailSchema, uuidSchema, urlSchema, guildIdSchema, nonEmptyString, formatZodErrors), all confirmed to have zero imports anywhere in the codebase. Campaign forms in Phase 27 will define their own schemas.

**DEBT-03: Wire ConnectionIssuesBanner to posts page**
In `src/app/(dashboard)/guilds/[guildId]/posts/page.tsx`:

1. Add import at the top with other component imports:
```typescript
import { ConnectionIssuesBanner } from '@/components/connection-issues-banner'
```

2. Place the banner after `<GuildTabs>` in the JSX, matching the accounts page pattern. The posts page already destructures `isError` and `data` from `usePostsInfinite`, so pass:
```tsx
<GuildTabs guildId={guildId} />
<ConnectionIssuesBanner isError={isError} hasData={!!data} />
```

Reference the accounts page (`src/app/(dashboard)/guilds/[guildId]/accounts/page.tsx`) for exact placement pattern -- the banner goes immediately after GuildTabs, before the main content.
  </action>
  <verify>
    <automated>cd /Users/gabrielleal/Desktop/dashboard-tracking-bot && test ! -f src/lib/validators.ts && npx tsc --noEmit && grep -q 'ConnectionIssuesBanner' src/app/\(dashboard\)/guilds/\[guildId\]/posts/page.tsx && echo "PASS"</automated>
  </verify>
  <done>validators.ts is deleted. ConnectionIssuesBanner is imported and rendered on the posts page with isError and hasData props. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extract centsToDisplay to shared format.ts and update imports</name>
  <files>src/lib/format.ts, src/hooks/use-bonus.ts, src/components/bonus/round-card.tsx, src/components/bonus/create-round-modal.tsx, src/components/bonus/payments-tab.tsx, src/components/bonus/results-tab.tsx, src/components/bonus/leaderboard-tab.tsx</files>
  <action>
**Step 1: Create src/lib/format.ts**
Create the new shared utility file with ONLY the centsToDisplay function (no speculative helpers -- per user decision):

```typescript
/**
 * Convert cents to dollar display string.
 * @example centsToDisplay(5000) // "$50.00"
 */
export function centsToDisplay(cents: number): string {
  return `$${(cents / 100).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`
}
```

**Step 2: Remove centsToDisplay from use-bonus.ts**
In `src/hooks/use-bonus.ts`, remove the `centsToDisplay` function definition (around lines 37-42) and its export. Do NOT add a re-export from format.ts -- per user decision, no indirection. Leave all other exports in use-bonus.ts intact.

**Step 3: Update all 5 bonus component imports**
In each of these 5 files, change the `centsToDisplay` import from `'@/hooks/use-bonus'` to `'@/lib/format'`. Each file imports other things from use-bonus too (hooks, types), so split the import -- keep the use-bonus import for everything else, add a separate import for centsToDisplay from format:

1. `src/components/bonus/round-card.tsx` (line ~7): Split `centsToDisplay` out of the use-bonus import, add `import { centsToDisplay } from '@/lib/format'`
2. `src/components/bonus/create-round-modal.tsx` (line ~11): Same pattern
3. `src/components/bonus/payments-tab.tsx` (line ~7): Same pattern
4. `src/components/bonus/results-tab.tsx` (line ~5): Same pattern
5. `src/components/bonus/leaderboard-tab.tsx` (line ~7): Same pattern

After updating all 5 files, verify no references to `centsToDisplay` remain pointing to `use-bonus`.
  </action>
  <verify>
    <automated>cd /Users/gabrielleal/Desktop/dashboard-tracking-bot && npx tsc --noEmit && test -f src/lib/format.ts && ! grep -rq "centsToDisplay.*use-bonus" src/components/bonus/ && ! grep -q "centsToDisplay" src/hooks/use-bonus.ts && grep -c "from '@/lib/format'" src/components/bonus/*.tsx | grep -q ":1" && echo "PASS"</automated>
  </verify>
  <done>src/lib/format.ts exists with centsToDisplay. use-bonus.ts no longer exports centsToDisplay. All 5 bonus components import centsToDisplay from @/lib/format. TypeScript compiles cleanly. No stale references to old import location.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- `src/lib/validators.ts` does not exist
- `src/lib/format.ts` exists and exports `centsToDisplay`
- `grep -r "centsToDisplay" src/` shows only format.ts definition and 5 bonus component imports from `@/lib/format`
- Posts page contains `ConnectionIssuesBanner` component
</verification>

<success_criteria>
1. validators.ts deleted with no compilation errors
2. ConnectionIssuesBanner renders on posts page with correct props
3. centsToDisplay lives in src/lib/format.ts and all 5 bonus imports updated
4. TypeScript compilation and build both succeed
</success_criteria>

<output>
After completion, create `.planning/phases/24-tech-debt-shared-utilities/24-02-SUMMARY.md`
</output>
