---
phase: 23-security-audit-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/AUDIT-REPORT.md
autonomous: true
requirements:
  - AUDIT-01

must_haves:
  truths:
    - "A comprehensive audit report exists at .planning/AUDIT-REPORT.md"
    - "The report covers all 10 OWASP Top 10 2021 categories with sub-findings"
    - "Every finding has a severity level, CWE ID, affected files, and evidence"
    - "Phase 17-22 hardening items are verified against actual code and marked Resolved with evidence"
    - "Open findings have fix priority (P1-P3) and accepted risks have inline justification"
    - "Performance findings section covers bundle, React Query, and memory concerns"
    - "Executive summary shows total findings count and risk distribution"
    - "OWASP posture summary table shows pass/partial/fail per category"
  artifacts:
    - path: ".planning/AUDIT-REPORT.md"
      provides: "Complete security and performance audit report"
      min_lines: 400
      contains: "OWASP Top 10 Posture Summary"
  key_links:
    - from: ".planning/AUDIT-REPORT.md"
      to: "src/proxy.ts"
      via: "Code evidence snippets"
      pattern: "proxy\\.ts"
    - from: ".planning/AUDIT-REPORT.md"
      to: "api/src/middleware/dashboardAuth.ts"
      via: "Backend audit findings"
      pattern: "dashboardAuth"
---

<objective>
Generate a comprehensive security and performance audit report covering the full hardened codebase (Phases 17-22 complete).

Purpose: Satisfy AUDIT-01 — document the post-hardening security posture with OWASP Top 10 coverage, CWE classification, risk scoring, and fix plans. This is the final deliverable of v1.2.

Output: `.planning/AUDIT-REPORT.md` — single Markdown file with executive summary, OWASP findings, performance findings, and recommendations.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-security-audit-report/23-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evidence gathering — npm audit, open question verification, spot-checks</name>
  <files>No files created — evidence gathered for Task 2</files>
  <action>
Run evidence-gathering commands and read source files to resolve open questions from RESEARCH.md. This task produces no files — it feeds Task 2.

1. Run `npm audit --json 2>/dev/null | head -200` in both repos:
   - Dashboard: `/Users/gabrielleal/Desktop/dashboard-tracking-bot`
   - Backend: `/Users/gabrielleal/Desktop/Tracking Data Bot/api`
   Record vulnerability counts by severity for the A06 section.

2. Read and verify open questions from RESEARCH.md:
   a. **Logout token revocation** — Read `src/app/api/auth/logout/route.ts` to verify whether it calls the backend to revoke tokens. If missing, this is an A07 High finding.
   b. **returnTo redirect validation** — Read `src/app/(auth)/login/page.tsx` (or wherever returnTo is consumed) to check if it validates returnTo as a relative path. If not validated, CWE-601 Medium finding.
   c. **Excel formula injection** — Read `src/lib/posts-excel-export.ts` to check if user-controlled cell content is escaped for formula injection (cells starting with =, +, -, @). If not escaped, CWE-1236 Medium/High finding.

3. Spot-check Phase 17-22 implementations by reading key files:
   - `src/proxy.ts` — verify CSRF HMAC generation (Phase 21), CSRF double-submit (pre-v1.2), CSP nonces, security headers
   - `src/lib/fetch-with-retry.ts` — verify split rate limits (Phase 19), 503 retry (Phase 19), error parsing (Phase 17)
   - `src/hooks/use-sse.ts` — verify heartbeat timeout 45s, generation counter (Phase 18)
   - `src/lib/server/backend-fetch.ts` — verify SSR auth forwarding (Phase 19)
   - `src/lib/server/security-headers.ts` — verify CSP configuration
   - `~/Desktop/Tracking Data Bot/api/src/middleware/dashboardAuth.ts` — verify JWT validation, guild access, admin check

Note all findings, code snippets for evidence, and resolved/open status for each item.
  </action>
  <verify>
    <automated>echo "Task 1 is evidence gathering only — verify Task 2 output"</automated>
  </verify>
  <done>All open questions from RESEARCH.md are resolved with actual code evidence. npm audit results recorded. Phase 17-22 spot-checks complete with code snippets ready for the report.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive audit report</name>
  <files>.planning/AUDIT-REPORT.md</files>
  <action>
Write the complete audit report to `.planning/AUDIT-REPORT.md` using evidence from Task 1 and the finding catalog from RESEARCH.md.

**Report structure (per locked decisions in CONTEXT.md):**

1. **Header**: Report date, codebase version (v1.2 post-Phases 17-22), scope (Next.js Dashboard + Express.js Backend API)

2. **Executive Summary**: Total findings count, risk distribution table (Critical/High/Medium/Low/Informational), overall posture assessment paragraph

3. **OWASP Top 10 Posture Summary Table**: One row per A01-A10 category with Pass/Partial/Fail status and finding count

4. **Security Findings** — organized by OWASP category (A01-A10), each with sub-findings using the locked format:
   - Title, Severity (Critical/High/Medium/Low/Informational), CWE-ID, Description, Affected files, Impact, Status (Resolved in Phase X / Open P1-P3 / Accepted Risk), Evidence (code snippet), Fix plan or Rationale

   Use the RESEARCH.md finding catalog as the primary source. Key findings per category:
   - **A01**: guildId without snowflake validation (Medium), returnTo redirect (result from Task 1), guild access control (Pass with evidence)
   - **A02**: HMAC CSRF (Resolved Ph.21), httpOnly cookies (Pass), dev-only insecure cookies (Accepted Risk), CSRF_HMAC_SECRET fallback (Low)
   - **A03**: Prisma parameterized queries (Pass), error message XSS (Resolved Ph.17), Excel formula injection (result from Task 1)
   - **A04**: Global mutable state in fetch-with-retry (Medium, Accepted Risk), single-process refresh dedup (Low), no structured logging (Low)
   - **A05**: CSP unsafe-inline for styles (Medium, Accepted Risk), CSP connect-src wss: (Informational), helmet config
   - **A06**: npm audit results from Task 1, bundle-analyzer version mismatch (Low), Zod version split (Informational)
   - **A07**: JWT validation (Pass), token revocation on logout (result from Task 1), CSRF per-request (Pass), auth rate limiting (Pass), cookie domain parsing (Low)
   - **A08**: No SRI (Low, deferred to v1.3), CSP reporting endpoint (Informational), CSRF HMAC integrity (Pass)
   - **A09**: Backend structured logging (Pass), dashboard console-only logging (Medium), no error tracking (Low)
   - **A10**: backendFetch fixed URL (Pass), OAuth redirect header (Low)

5. **Performance Findings** — organized by codebase area:
   - Bundle/cold start: optimizePackageImports (Resolved Ph.22), dynamic imports (Resolved Ph.22), bundle-analyzer version mismatch
   - React Query: staleTime normalization (Resolved Ph.22), 4-tier staleTime system
   - Memory: unbounded infinite scroll cache (Medium), Excel in-memory ZIP (Low), SSE connection scaling (Low)
   - Re-render cycles (Informational)

   Each performance finding uses same format with CWE-400 or CWE-770 as applicable.

6. **Recommendations Roadmap** — brief P1/P2/P3 priority list of open findings, noting deployment dependencies (backend Phases 35-39 status)

**Writing guidelines:**
- Every "Resolved in Phase X" finding MUST include a code snippet as evidence (use snippets from RESEARCH.md and Task 1 spot-checks)
- Every "Accepted Risk" finding MUST include a rationale paragraph explaining why the risk is accepted
- Every open finding MUST have a fix priority (P1/P2/P3) but NO time estimates
- Both codebases must have findings — do NOT skip backend (locked decision)
- All OWASP categories get equal depth (locked decision)
- CWE IDs on every finding including performance (locked decision)
  </action>
  <verify>
    <automated>test -f /Users/gabrielleal/Desktop/dashboard-tracking-bot/.planning/AUDIT-REPORT.md && echo "EXISTS" && grep -c "^### A0" /Users/gabrielleal/Desktop/dashboard-tracking-bot/.planning/AUDIT-REPORT.md && grep -c "CWE-" /Users/gabrielleal/Desktop/dashboard-tracking-bot/.planning/AUDIT-REPORT.md && grep -c "Severity" /Users/gabrielleal/Desktop/dashboard-tracking-bot/.planning/AUDIT-REPORT.md</automated>
  </verify>
  <done>
- AUDIT-REPORT.md exists at .planning/AUDIT-REPORT.md
- Contains all 10 OWASP sections (A01-A10) with sub-findings
- Every finding has Severity, CWE, Description, Affected files, Impact, Status, Evidence
- Executive summary shows total findings and risk distribution
- OWASP posture summary table present
- Performance findings section present with CWE IDs
- Both dashboard and backend codebases represented in findings
- Phase 17-22 resolved items have code snippet evidence
- Open findings have P1-P3 priority
- Accepted risks have inline rationale
  </done>
</task>

</tasks>

<verification>
1. File exists: `test -f .planning/AUDIT-REPORT.md`
2. All OWASP categories covered: `grep -c "^### A0" .planning/AUDIT-REPORT.md` returns 10
3. CWE IDs present: `grep -c "CWE-" .planning/AUDIT-REPORT.md` returns 15+
4. Findings have severity: `grep -c "Severity" .planning/AUDIT-REPORT.md` returns 15+
5. Performance section exists: `grep -c "Performance" .planning/AUDIT-REPORT.md` returns 2+
6. Executive summary exists: `grep "Executive Summary" .planning/AUDIT-REPORT.md`
7. Posture table exists: `grep "Posture Summary" .planning/AUDIT-REPORT.md`
</verification>

<success_criteria>
- AUDIT-REPORT.md exists and covers OWASP Top 10, CWE classification, and performance findings
- Every finding has a risk score and documented fix plan or accepted-risk justification
- The report reflects the post-hardening state (Phases 17-22 complete)
- Both Next.js dashboard and Express.js backend are audited
- npm audit results included for both repos
</success_criteria>

<output>
After completion, create `.planning/phases/23-security-audit-report/23-01-SUMMARY.md`
</output>
