---
phase: 26-campaign-list-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/campaigns/campaign-status-badge.tsx
  - src/components/campaigns/campaign-card.tsx
  - src/components/campaigns/campaign-card-skeleton.tsx
  - src/components/campaigns/campaign-status-select.tsx
  - src/components/guild-tabs.tsx
  - src/app/(dashboard)/guilds/[guildId]/campaigns/page.tsx
autonomous: true
requirements: [CAMP-01, CAMP-02, CAMP-04]

must_haves:
  truths:
    - "User can scroll through campaigns with infinite scroll loading more results at bottom"
    - "User can filter the campaign list by status and the list updates immediately"
    - "Campaign status is displayed as a color-coded pill badge on list cards"
    - "Campaigns tab appears in guild navigation"
  artifacts:
    - path: "src/components/campaigns/campaign-status-badge.tsx"
      provides: "Color-coded pill badge for 5 campaign statuses"
      exports: ["CampaignStatusBadge"]
    - path: "src/components/campaigns/campaign-card.tsx"
      provides: "Card component showing name, status, budget, counts, date"
      exports: ["CampaignCard"]
    - path: "src/components/campaigns/campaign-card-skeleton.tsx"
      provides: "Skeleton loading card for campaign list"
      exports: ["CampaignCardSkeleton"]
    - path: "src/components/campaigns/campaign-status-select.tsx"
      provides: "Headless UI Listbox dropdown for 5 statuses + All"
      exports: ["CampaignStatusSelect"]
    - path: "src/app/(dashboard)/guilds/[guildId]/campaigns/page.tsx"
      provides: "Campaign list page with infinite scroll and filtering"
      exports: ["default"]
  key_links:
    - from: "src/app/(dashboard)/guilds/[guildId]/campaigns/page.tsx"
      to: "useCampaignsInfinite"
      via: "hook call with guildId and optional status filter"
      pattern: "useCampaignsInfinite\\(guildId"
    - from: "src/app/(dashboard)/guilds/[guildId]/campaigns/page.tsx"
      to: "useInView"
      via: "intersection observer sentinel for infinite scroll"
      pattern: "useInView|ref.*inView"
    - from: "src/components/guild-tabs.tsx"
      to: "/campaigns"
      via: "new tab entry in tabs array"
      pattern: "Campaigns.*campaigns"
---

<objective>
Build the campaign list page with shared campaign components: status badge, card, skeleton, status filter dropdown, and GuildTabs "Campaigns" entry. Users can browse campaigns with infinite scroll and filter by status.

Purpose: Provides the primary campaign browsing experience and creates shared components reused by the detail page in plan 02.
Output: Functional campaign list page at `/guilds/[guildId]/campaigns/` with 4 reusable campaign components.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-campaign-list-detail/26-CONTEXT.md
@.planning/phases/26-campaign-list-detail/26-RESEARCH.md

@src/types/campaign.ts
@src/hooks/use-campaigns.ts
@src/components/guild-tabs.tsx
@src/components/filters/status-select.tsx
@src/components/empty-state.tsx
@src/components/platform-icon.tsx
@src/components/ui/skeleton.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/campaign.ts:
```typescript
export type CampaignStatus = 'Draft' | 'Active' | 'Paused' | 'SubmissionsClosed' | 'Completed'

export interface Campaign {
  id: string
  createdAt: string
  name: string
  status: CampaignStatus
  brandId: string
  budgetCents: number
  _count: { posts: number; participants: number }
}

export interface CampaignsResponse {
  campaigns: Campaign[]
  nextCursor: string | null
}
```

From src/hooks/use-campaigns.ts:
```typescript
export function useCampaignsInfinite(guildId: string, status?: CampaignStatus): UseInfiniteQueryResult<CampaignsResponse>
```

From src/components/filters/status-select.tsx:
```typescript
// Clone this exact Listbox pattern for CampaignStatusSelect
// Uses: Listbox, ListboxButton, ListboxOptions, ListboxOption from @headlessui/react
// Styling: bg-surface border border-border rounded-lg, data-[focus]:bg-background
```

From src/components/empty-state.tsx:
```typescript
export function EmptyState({ icon, title, description, action }: EmptyStateProps): JSX.Element
export function NoResults({ query, onClear }: NoResultsProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared campaign components (badge, card, skeleton, status select)</name>
  <files>
    src/components/campaigns/campaign-status-badge.tsx,
    src/components/campaigns/campaign-card.tsx,
    src/components/campaigns/campaign-card-skeleton.tsx,
    src/components/campaigns/campaign-status-select.tsx
  </files>
  <action>
Create 4 components in `src/components/campaigns/`:

**campaign-status-badge.tsx** -- Reusable pill badge mapping CampaignStatus to colors:
- Draft: bg-gray-500 text-gray-800
- Active: bg-green-500 text-green-900
- Paused: bg-yellow-500 text-yellow-900
- SubmissionsClosed: bg-orange-500 text-orange-900 (display label: "Submissions Closed")
- Completed: bg-blue-500 text-blue-900
- Styling: rounded-full px-2.5 py-0.5 text-xs font-medium (pill shape per user decision)
- Props: `{ status: CampaignStatus; className?: string }`
- Use a `STATUS_STYLES` record with `bg`, `text`, and `label` fields per status

**campaign-card.tsx** -- List card component:
- Props: `{ campaign: Campaign; guildId: string }`
- Wraps in Next.js `Link` to `/guilds/${guildId}/campaigns/${campaign.id}`
- Styling: `bg-surface border border-border rounded-lg p-4 hover:bg-surface-hover/30 transition-colors` (matches RoundCard pattern)
- Layout: top row = campaign name (font-semibold text-white truncate) + CampaignStatusBadge right-aligned
- Below: budget via `centsToDisplay(campaign.budgetCents)`, post count + participant count in flex row, createdAt formatted with `format(new Date(campaign.createdAt), 'MMM d, yyyy')` from date-fns
- Do NOT show brand name on list cards -- `Campaign` type has only `brandId`, not brand label. Brand name is visible on detail page.
- Import `centsToDisplay` from `@/lib/format`, `format` from `date-fns`

**campaign-card-skeleton.tsx** -- Skeleton card matching card layout:
- Uses the `Skeleton` component from `@/components/ui/skeleton`
- Mimics campaign-card layout: title bar + badge skeleton on top, 3 lines of info below
- Same container styling (bg-surface border border-border rounded-lg p-4)

**campaign-status-select.tsx** -- Clone of `StatusSelect` adapted for campaign statuses:
- Clone the exact Headless UI Listbox pattern from `src/components/filters/status-select.tsx`
- Replace statuses array with: '' (All Statuses), Draft, Active, Paused, SubmissionsClosed (label: "Submissions Closed"), Completed
- Props: `{ value: string; onChange: (value: string) => void; className?: string }`
- Keep all ARIA, keyboard nav, focus ring, and styling identical to StatusSelect
- Width can be slightly wider (w-44 instead of w-36) to fit "Submissions Closed"

All components must be 'use client' where they use hooks/events. Badge and skeleton can be server-compatible (no 'use client' needed).
  </action>
  <verify>
    <automated>cd /Users/gabrielleal/Desktop/dashboard-tracking-bot && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Four campaign components exist with correct types, imports, and styling. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build campaign list page with infinite scroll, status filter, and GuildTabs entry</name>
  <files>
    src/components/guild-tabs.tsx,
    src/app/(dashboard)/guilds/[guildId]/campaigns/page.tsx
  </files>
  <action>
**guild-tabs.tsx** -- Add Campaigns tab to the tabs array:
- Insert `{ name: 'Campaigns', href: '/campaigns' }` after 'Brands' in the tabs array
- No other changes to this file

**campaigns/page.tsx** -- Full campaign list page:
- 'use client' component
- Extract `guildId` from params using `use(params)` pattern (Next.js App Router: `params: Promise<{ guildId: string }>`)
- State: `const [status, setStatus] = useState<string>('')`
- Hook: `useCampaignsInfinite(guildId, status ? status as CampaignStatus : undefined)`
- Infinite scroll: use `useInView` from `react-intersection-observer` with `{ threshold: 0, rootMargin: '100px' }`. In useEffect, call `fetchNextPage()` when `inView && hasNextPage && !isFetchingNextPage`
- Flatten pages: `const campaigns = data?.pages.flatMap(page => page.campaigns) ?? []`

Page layout (top to bottom):
1. Page header: `<h1>` "Campaigns" in text-2xl font-bold text-white
2. Filter bar: `CampaignStatusSelect` component in a flex row below the header
3. Content area with conditional rendering:
   - Loading (isLoading): render 3 `CampaignCardSkeleton` components
   - Error (isError): inline error with "Failed to load campaigns" message + "Try again" button calling `refetch()`. Also render `ConnectionIssuesBanner` at top if available (import from `@/components/connection-issues-banner` -- check if it exists first; if not, skip it)
   - Empty (campaigns.length === 0 and not loading):
     - If status filter is active: `<NoResults query={statusLabel} onClear={() => setStatus('')} />` where statusLabel is the display label for current status (e.g., "Submissions Closed" for SubmissionsClosed)
     - If no filter: `<EmptyState icon={<Megaphone className="w-16 h-16" />} title="No campaigns yet" description="Create one to get started." />` (use Megaphone from lucide-react, no action button per user decision)
   - Data: map `campaigns` to `CampaignCard` components in a `space-y-3` container
4. Infinite scroll sentinel: `<div ref={ref} />` after the card list
5. Loading more indicator: show a small spinner or "Loading..." text when `isFetchingNextPage` is true

Server-side status filtering: Do NOT use useMemo for client-side filtering. The `useCampaignsInfinite` hook already passes `status` param to the API. When status changes, the hook refetches with the new param.

Import `useInView` from `react-intersection-observer`, `use` and `useState`/`useEffect` from `react`.
  </action>
  <verify>
    <automated>cd /Users/gabrielleal/Desktop/dashboard-tracking-bot && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Campaign list page renders at /guilds/[guildId]/campaigns/ with infinite scroll, status filtering, loading skeletons, empty states, and error handling. GuildTabs shows "Campaigns" tab after "Brands".</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Navigate to `/guilds/{guildId}/campaigns/` -- page renders with GuildTabs showing Campaigns tab active
- Campaign cards display with name, status badge (colored pill), budget, post/participant counts, date
- Status filter dropdown shows 6 options (All + 5 statuses) and re-fetches list on change
- Scrolling to bottom triggers loading of next page (infinite scroll)
- Empty state shows when no campaigns exist
- NoResults with "Clear filters" shows when status filter has no results
</verification>

<success_criteria>
- Campaign list page is fully functional with infinite scroll and status filtering
- 4 shared campaign components are created and reusable
- GuildTabs includes "Campaigns" after "Brands"
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-campaign-list-detail/26-01-SUMMARY.md`
</output>
