---
phase: 22-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - src/hooks/use-audit-log.ts
  - src/hooks/use-bonus.ts
  - src/hooks/use-exports.ts
  - src/hooks/use-guilds.ts
  - src/hooks/use-alerts.ts
  - src/hooks/use-sessions.ts
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "next.config.mjs declares optimizePackageImports for lucide-react and recharts"
    - "No hook has staleTime: 30 * 1000 on a non-real-time list query"
    - "Hooks with refetchInterval retain their existing staleTime unchanged"
    - "npm run build succeeds without errors"
  artifacts:
    - path: "next.config.mjs"
      provides: "optimizePackageImports experimental config"
      contains: "optimizePackageImports"
    - path: "src/hooks/use-audit-log.ts"
      provides: "staleTime raised from 30s to 2min"
      contains: "2 * 60 * 1000"
    - path: "src/hooks/use-bonus.ts"
      provides: "staleTime raised from 30s to 2min on 4 queries"
      contains: "2 * 60 * 1000"
  key_links:
    - from: "next.config.mjs"
      to: "Next.js build pipeline"
      via: "experimental.optimizePackageImports config key"
      pattern: "optimizePackageImports.*lucide-react"
---

<objective>
Configure package import optimization and normalize React Query staleTime values across all hooks.

Purpose: Reduce dev server cold start (lucide-react module scan drops from ~1583 to ~333 modules) and eliminate redundant background refetches when navigating between pages.
Output: Updated next.config.mjs with optimizePackageImports; 6 hook files with normalized staleTime values.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-performance-optimization/22-RESEARCH.md

@next.config.mjs
@src/app/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimizePackageImports to next.config.mjs</name>
  <files>next.config.mjs</files>
  <action>Add an `experimental` block to the `nextConfig` object in next.config.mjs with `optimizePackageImports: ['lucide-react', 'recharts']`. The config currently has no `experimental` key — add it as the first property in the nextConfig object, before `images`. Do NOT modify the existing withBundleAnalyzer wrapper, images config, or headers config. The result should be:

```javascript
const nextConfig = {
    experimental: {
        optimizePackageImports: ['lucide-react', 'recharts'],
    },
    images: { /* ... existing unchanged ... */ },
    async headers() { /* ... existing unchanged ... */ },
};
```

Both packages are already on Next.js 16's default optimization list, but explicit config satisfies PERF-01 as written and makes the setting auditable.</action>
  <verify>
    <automated>grep -q "optimizePackageImports" /Users/gabrielleal/Desktop/dashboard-tracking-bot/next.config.mjs && echo "PASS: optimizePackageImports found" || echo "FAIL"</automated>
  </verify>
  <done>next.config.mjs contains experimental.optimizePackageImports with both lucide-react and recharts listed</done>
</task>

<task type="auto">
  <name>Task 2: Normalize staleTime across React Query hooks</name>
  <files>src/hooks/use-audit-log.ts, src/hooks/use-bonus.ts, src/hooks/use-exports.ts, src/hooks/use-guilds.ts, src/hooks/use-alerts.ts, src/hooks/use-sessions.ts</files>
  <action>Raise `staleTime: 30 * 1000` to `staleTime: 2 * 60 * 1000` on list queries that do NOT have a `refetchInterval`. These are paginated/infrequently-changing data where 30s causes unnecessary background refetches on navigation.

Specific changes (file: line reference from grep):

1. **use-audit-log.ts:26** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — audit entries don't change in real time`

2. **use-bonus.ts:74** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — round list`
3. **use-bonus.ts:148** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — round detail`
4. **use-bonus.ts:171** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — results`
5. **use-bonus.ts:191** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — leaderboard`

6. **use-exports.ts:125** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — export history`

7. **use-alerts.ts:46** — `staleTime: 30 * 1000` -> `staleTime: 2 * 60 * 1000, // 2 minutes — alert thresholds`

**DO NOT change these hooks — they have refetchInterval set (staleTime change is low-impact):**
- use-guilds.ts:64 (staleTime: 30s + refetchInterval: 60s) — leave as-is
- use-guilds.ts:118 (staleTime: 30s + refetchInterval conditional) — leave as-is
- use-sessions.ts:27 (staleTime: 30s + refetchInterval: 60s) — leave as-is

**DO NOT change hooks already at 1min+ values** (use-email-alerts, use-trash, use-tracking, use-analytics, use-user — all already at 1min or higher).</action>
  <verify>
    <automated>grep -rn "staleTime: 30 \* 1000" /Users/gabrielleal/Desktop/dashboard-tracking-bot/src/hooks/ | grep -v "refetchInterval" | grep -v "use-guilds" | grep -v "use-sessions" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS: no 30s staleTime on non-polling hooks" || echo "FAIL: still found 30s staleTime"</automated>
  </verify>
  <done>All non-real-time list hooks use staleTime >= 2 minutes. Hooks with refetchInterval retain their 30s staleTime unchanged. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `grep -r "optimizePackageImports" next.config.mjs` returns the config line
2. `grep -rn "staleTime: 30 \* 1000" src/hooks/` returns ONLY hooks that have refetchInterval (use-guilds, use-sessions)
3. `npm run build` completes without errors
</verification>

<success_criteria>
- next.config.mjs has experimental.optimizePackageImports with lucide-react and recharts
- 7 staleTime values raised from 30s to 2min across 5 hook files
- 3 hooks with refetchInterval left unchanged
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/22-performance-optimization/22-01-SUMMARY.md`
</output>
