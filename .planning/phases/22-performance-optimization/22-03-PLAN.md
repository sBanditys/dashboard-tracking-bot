---
phase: 22-performance-optimization
plan: "03"
type: execute
wave: 1
depends_on: ["22-02"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [PERF-03]

must_haves:
  truths:
    - "ANALYZE=true npm run build completes successfully and produces bundle analysis output"
    - "CreateRoundModal, LeaderboardTab, and EmailConfigSection appear as separate chunks in the analyzer output"
    - "A confirmation note exists in the plan SUMMARY documenting the bundle analysis results"
  artifacts:
    - path: ".next/analyze/"
      provides: "Bundle analysis HTML treemap report (gitignored, ephemeral)"
  key_links: []
---

<objective>
Run `ANALYZE=true npm run build` to produce the bundle analysis report artifact required by PERF-03, confirm the three dynamically-imported components (CreateRoundModal, LeaderboardTab, EmailConfigSection) appear as separate chunks, and document the results in the SUMMARY.

Purpose: Close the PERF-03 verification gap — dynamic imports are already applied but the formal bundle analysis run was never executed. The `.next/analyze/` directory is gitignored, so the deliverable is a documented confirmation in the SUMMARY.
Output: Bundle analysis run completed; SUMMARY documents chunk confirmation.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-performance-optimization/22-02-SUMMARY.md
@.planning/phases/22-performance-optimization/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bundle analysis build and confirm dynamic import chunks</name>
  <files></files>
  <action>
Run the bundle analysis build. Try the standard command first:

```bash
ANALYZE=true npm run build
```

If this fails or produces no `.next/analyze/` output (because Turbopack is the default bundler in Next.js 16 and @next/bundle-analyzer wraps webpack config), retry with the --no-turbo flag:

```bash
ANALYZE=true npm run build -- --no-turbo
```

After the build completes, verify the `.next/analyze/` directory exists (or check for `.next/analyze/client.html`). If the analyzer opens browser tabs instead of writing files, check for the generated HTML files in `.next/analyze/` or the build output directory.

Then inspect the client bundle output to confirm that `CreateRoundModal`, `LeaderboardTab`, and `EmailConfigSection` appear as separate chunks. Methods to confirm:
1. If `.next/analyze/client.html` exists, grep or read it for the component names.
2. If no HTML file, check `.next/static/chunks/` for chunk filenames that correspond to the dynamically-imported component paths (e.g., chunks containing `create-round-modal`, `leaderboard-tab`, `email-config-section`).
3. As a fallback, inspect the build output log for chunk details.

Record the findings: which components are separate chunks, their approximate sizes, and the total client bundle size reduction compared to what a non-dynamic build would produce.

Important: The `.next/` directory is gitignored. Do NOT attempt to commit any files from `.next/`. The deliverable from this task is the information gathered, which goes into the SUMMARY.
  </action>
  <verify>
    <automated>ls -la .next/analyze/ 2>/dev/null || ls .next/static/chunks/ | grep -E "create-round-modal|leaderboard-tab|email-config-section" | head -5</automated>
  </verify>
  <done>Bundle analysis build ran successfully. The three dynamically-imported components (CreateRoundModal, LeaderboardTab, EmailConfigSection) are confirmed as separate chunks in the client bundle. Results documented for SUMMARY.</done>
</task>

</tasks>

<verification>
- `ANALYZE=true npm run build` (or with `--no-turbo`) completes without error
- Bundle analysis output confirms separate chunks for the three dynamic-imported components
- No files committed from `.next/` (gitignored)
</verification>

<success_criteria>
- The bundle analysis build has been executed at least once
- CreateRoundModal, LeaderboardTab, and EmailConfigSection are confirmed as separate client chunks
- The SUMMARY documents the chunk sizes and confirms PERF-03 is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/22-performance-optimization/22-03-SUMMARY.md`
</output>
