---
phase: 03-tracking-data-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/hooks/use-debounce.ts
  - src/components/platform-icon.tsx
  - src/components/empty-state.tsx
autonomous: true

must_haves:
  truths:
    - "New dependencies are installed and available for import"
    - "Platform icons render consistently across the app"
    - "Empty state shows when list has no data"
  artifacts:
    - path: "src/hooks/use-debounce.ts"
      provides: "Debounce hook for search input"
      exports: ["useDebounce"]
    - path: "src/components/platform-icon.tsx"
      provides: "Reusable platform icon component"
      exports: ["PlatformIcon"]
    - path: "src/components/empty-state.tsx"
      provides: "Generic empty state with illustration and CTA"
      exports: ["EmptyState", "NoResults"]
  key_links:
    - from: "src/components/platform-icon.tsx"
      to: "platform types"
      via: "platform prop matching type definition"
      pattern: "platform.*instagram|tiktok|youtube|x"
---

<objective>
Set up foundation components and dependencies for Phase 3 tracking data display.

Purpose: Establish shared utilities and components that all subsequent plans depend on - debounce hook for filtering, platform icons for cards, and empty states for all lists.

Output: Installed dependencies (react-intersection-observer, react-day-picker, date-fns), useDebounce hook, PlatformIcon component, EmptyState/NoResults components.
</objective>

<execution_context>
@/Users/gabrielleal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielleal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tracking-data-display/03-CONTEXT.md
@.planning/phases/03-tracking-data-display/03-RESEARCH.md

# Existing platform icons to extract (duplicated in both files)
@src/app/(dashboard)/guilds/[guildId]/accounts/page.tsx
@src/app/(dashboard)/guilds/[guildId]/posts/page.tsx

# Existing patterns
@src/components/ui/skeleton.tsx
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create useDebounce hook</name>
  <files>
    package.json
    src/hooks/use-debounce.ts
  </files>
  <action>
1. Install new dependencies:
   ```bash
   npm install react-intersection-observer react-day-picker date-fns
   ```

2. Create src/hooks/use-debounce.ts:
   - Generic hook that debounces any value
   - Default delay of 300ms
   - Proper cleanup on unmount
   - Pattern from 03-RESEARCH.md Pattern 2
  </action>
  <verify>
- `npm ls react-intersection-observer react-day-picker date-fns` shows all installed
- Import useDebounce in a test file compiles without error
  </verify>
  <done>Dependencies installed and useDebounce hook exports correctly</done>
</task>

<task type="auto">
  <name>Task 2: Extract PlatformIcon to shared component</name>
  <files>
    src/components/platform-icon.tsx
  </files>
  <action>
1. Create src/components/platform-icon.tsx:
   - Extract the 4 SVG icons (Instagram, TikTok, YouTube, X) from accounts/page.tsx
   - Accept platform prop matching the type: 'instagram' | 'tiktok' | 'youtube' | 'x'
   - Include platform-specific colors:
     - instagram: text-pink-500
     - tiktok: text-gray-400
     - youtube: text-red-500
     - x: text-gray-400
   - Optional size prop defaulting to "w-5 h-5"
   - Export single PlatformIcon component
   - Handle unknown platform with gray link icon fallback

Pattern:
```tsx
interface PlatformIconProps {
  platform: 'instagram' | 'tiktok' | 'youtube' | 'x'
  size?: string
}

export function PlatformIcon({ platform, size = 'w-5 h-5' }: PlatformIconProps) {
  // SVG icons with platform-specific colors
}
```
  </action>
  <verify>
- Component exports without TypeScript errors
- All 4 platform icons render with correct colors
  </verify>
  <done>PlatformIcon component provides consistent icons across app</done>
</task>

<task type="auto">
  <name>Task 3: Create EmptyState and NoResults components</name>
  <files>
    src/components/empty-state.tsx
  </files>
  <action>
1. Create src/components/empty-state.tsx with two exports:

EmptyState - For when list is truly empty (no data):
- icon: React.ReactNode (flexible for any icon)
- title: string
- description: string
- action?: { label: string, href: string } - CTA button linking to docs
- Centered layout with padding, dark theme styling
- Pattern from 03-RESEARCH.md Code Examples

NoResults - For when filter returns no matches:
- query: string (the search term that matched nothing)
- onClear: () => void (clear filters callback)
- Shows "No results for [query]" with clear filters button
- Simpler, inline appearance

Styling:
- EmptyState: Large icon (w-16 h-16), py-16 padding, centered text
- NoResults: Smaller (py-8), text-gray-400, accent-purple link for clear
- Both use dark theme colors (text-white, text-gray-400, bg-accent-purple)
  </action>
  <verify>
- Both components export without TypeScript errors
- EmptyState renders with action button when provided
- NoResults shows query text and clear button
  </verify>
  <done>Empty states ready for accounts, posts, and brands pages</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - no TypeScript errors
2. All new files exist in correct locations
3. Dependencies appear in package.json and are importable
</verification>

<success_criteria>
- react-intersection-observer, react-day-picker, date-fns installed
- useDebounce hook works with any value type
- PlatformIcon renders all 4 platforms with correct colors
- EmptyState shows icon, title, description, optional action
- NoResults shows query and clear button
</success_criteria>

<output>
After completion, create `.planning/phases/03-tracking-data-display/03-01-SUMMARY.md`
</output>
